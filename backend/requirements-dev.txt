# Development and CI tooling. Not needed to run the app or give the demo.
#
#   pip install -r requirements-dev.txt
#
# ruff is a Rust binary: on a Python with no matching prebuilt wheel, pip will
# try to build it from source and fail unless a Rust toolchain is present.
# That is exactly why it is not in requirements.txt.

-r requirements.txt

pytest>=9.0.3,<10.0
pytest-asyncio>=0.24,<1.0
respx>=0.21,<1.0
ruff>=0.6
pyright>=1.1.383
